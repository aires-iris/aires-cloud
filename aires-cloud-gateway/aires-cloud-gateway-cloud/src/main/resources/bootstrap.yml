spring:
  application:
    name: aires-cloud-gateway-cloud
  cloud:
    nacos:
      discovery:
        server-addr: ${nacos-url}:8001
      config:
        server-addr: ${nacos-url}:8001
        group: DEFAULT_GROUP
        prefix: aires-cloud-gateway-cloud
        file-extension: yaml